apiVersion: apps/v1
kind: Deployment
metadata:
  name: ubunturequests
spec:
  replicas: 2
  template:
    spec:
      containers:
      - name: nginx
        image: nginx:1.14.2
        ports:
        - containerPort: 80
